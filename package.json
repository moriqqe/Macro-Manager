{
  "name": "macro-manager",
  "version": "0.1.0-beta.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node scripts/ensure-release-windows.cjs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0"
  }
}
